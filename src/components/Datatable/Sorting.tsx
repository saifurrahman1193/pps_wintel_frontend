function Sorting({ column, table, order }) {
    return (
        <>
            <i className='mdi mdi-sort-alphabetical-ascending' title="Click to sort" style={{ cursor: 'pointer' }}/>
        </>
    )
}

export default Sorting
